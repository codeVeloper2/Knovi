# Learning Room Robot Fix

Fixed the Small Steps / Big Progress promo card so its robot artwork is visible.
The promo now uses a dedicated artwork viewport, clears the inherited `right` positioning,
and hides the speech bubble in the compact card so the robot remains fully visible.
Responsive sizes are provided for desktop, tablet, and mobile.
