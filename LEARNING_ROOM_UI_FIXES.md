# Learning Room UI fixes — 3.2

- Subject cards now use the available desktop content width instead of leaving an empty right rail.
- Large desktop uses four subject cards per row; medium desktop uses three.
- The Small Steps / Big Progress robot is scaled and centered inside its artwork column so the full robot remains visible instead of being cropped.
- Mobile keeps the existing two-column subject layout and uses a smaller, contained robot treatment.
